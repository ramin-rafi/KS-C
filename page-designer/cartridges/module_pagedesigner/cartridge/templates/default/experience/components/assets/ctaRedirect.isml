<isscript>
    var assets = require('*/cartridge/scripts/assets.js');
    // assets.addCss('/css/component.css');
</isscript>

<a class="cta" href="${pdict.ctaLink}" target="_blank">
    <img src="${pdict.ctaImage}" alt="${pdict.imageAlt}" />

    <isif condition="${pdict.ctaOverlay}">
        <span class="cta-overlay">${pdict.ctaOverlay}</span>
    </isif>
</a>