<isscript>
    var assets = require('*/cartridge/scripts/assets.js');
    assets.addCss('/css/component.css');
</isscript>

<div id="accordion-${pdict.groupName}"
    class="${pdict.colPadding ? 'col ' + pdict.colPadding : 'col'} ${pdict.addClass ? 'accordion ' + pdict.addClass : 'accordion'}">
    <isif condition="${pdict.groupHeading}">
        <h1>${pdict.groupHeading}</h1>
    </isif>

    <isprint value="${pdict.regions.accordion.render()}" encoding="off" />
</div>